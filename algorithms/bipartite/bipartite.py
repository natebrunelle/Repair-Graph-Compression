#bipartite assignment
from bipartite import bipartite_node


def bipartite(c_graph):
    queue = [] #initialize a queue
    for node in c_graph:
        temp = bipartite_node()
        temp.vertex(node)





    return False # if the graph is not a repait bipartite graph